{
  "name": "maintenance-union-irbid",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --check app.js",
    "test": "node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
